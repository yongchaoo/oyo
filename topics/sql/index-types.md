# 索引的类型与使用场景

### 标准答案

1. **B+ 树索引**：最常用，适合范围查询和排序，MySQL InnoDB 默认索引结构
2. **哈希索引**：等值查询 O(1)，不支持范围查询，Memory 引擎支持
3. **全文索引**：用于文本搜索，MATCH AGAINST 语法
4. **联合索引**：多列组成，遵循最左前缀原则

### 追问点

- 聚簇索引 vs 非聚簇索引：聚簇索引叶子节点存数据行，非聚簇存主键值（回表）
- 覆盖索引：查询字段都在索引中，无需回表
- 索引失效场景：LIKE '%xx'、对列使用函数、隐式类型转换、OR 连接非索引列
